TODO:

  - Mention WebKit's `displayName`
  - Mention `arguments.callee` being deprecated in ES5
  - Explain Spidermonkey's function statements
  - Explain `f = function f(){ ... }` memory usage
  - Explain why `(function(){ f = function f(){ }; })();` looks like undeclared assignment but doesn't declare global `f`